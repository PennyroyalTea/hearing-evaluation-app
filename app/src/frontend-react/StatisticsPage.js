import React from "react";

import {Empty} from 'antd'


export default class StatisticsPage extends React.Component {
    render() {
        return (
            <Empty description={'Здесь будет страница с историей всех запусков в режиме тестирования'} />
        )
    }
}